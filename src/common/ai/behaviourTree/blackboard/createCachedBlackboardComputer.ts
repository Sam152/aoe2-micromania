import { BlackboardComputer } from "./computeBlackboard.ts";

export function createCachedBlackboardComputer(): BlackboardComputer {
  // use blackboardComputer and create a cached version of the given type.
}
